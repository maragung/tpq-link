name: tpq_futuhil_hidayah
description: Aplikasi Manajemen TPQ Futuhil Hidayah Wal Hikmah
publish_to: 'none'
version: 1.0.12+13

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.0
  provider: ^6.1.1
  intl: ^0.19.0
  mobile_scanner: ^5.1.1
  uni_links: ^0.5.1
  flutter_secure_storage: ^9.0.0
  shimmer: ^3.0.0
  crypto: ^3.0.0
  local_auth: ^2.3.0
  workmanager: ^0.5.2
  connectivity_plus: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/
